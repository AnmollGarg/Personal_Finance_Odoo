<odoo>
    <!-- List View -->
    <record id="view_personal_finance_bank_list" model="ir.ui.view">
        <field name="name">personal.finance.bank.list</field>
        <field name="model">personal.finance.bank</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list>
                <field name="bank_name"/>
                <field name="account_number"/>
                <field name="account_holder_name"/>
                <field name="account_type"/>
                <field name="balance"/>
                <field name="ifsc_code"/>
                <field name="bank_branch"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_personal_finance_bank_form" model="ir.ui.view">
        <field name="name">personal.finance.bank.form</field>
        <field name="model">personal.finance.bank</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="bank_name" placeholder="Bank of Baroda"/>
                        <field name="account_number" placeholder="XXXX XXXX XXXX XXXX"/>
                        <field name="account_holder_name" placeholder="Harry"/>
                    </group>
                    <group>
                        <field name="account_type" widget="radio" options="{'horizontal': true}"/>
                        <field name="balance" placeholder="00.00"/>
                    </group>
                    <group>
                        <field name="ifsc_code" placeholder="IFSC code"/>
                        <field name="bank_branch" placeholder="Harries Park"/>
                    </group>
                </sheet>
                  <chatter>
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="activity_ids"/>
                    <field name="message_ids" widget="mail_thread"/>
                </chatter >
            </form>
        </field>
    </record>
</odoo>